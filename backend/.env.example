# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Docker PostgreSQL)
# If using Docker Compose, use these settings:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=test_automation_hub
DB_USER=postgres
DB_PASSWORD=postgres

# If using local PostgreSQL installation, change DB_PASSWORD to your password

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=http://localhost:5173
